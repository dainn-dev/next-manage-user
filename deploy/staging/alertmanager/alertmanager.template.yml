route:
  receiver: approved-incident-receiver
  group_by: [alertname, severity, owner]
  group_wait: 5s
  group_interval: 30s
  repeat_interval: 4h
receivers:
  - name: approved-incident-receiver
    webhook_configs:
      - url: "__ALERT_WEBHOOK_URL__"
        send_resolved: true
